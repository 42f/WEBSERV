Brian :
        ✅ fix location resolver pour /
        ✅ check autoindex with non existing pouette.html -> ok 404 returned
        🔴 operator= for every socket componenet
        ✅ check redirect variables
        ✅ autoindex: test
        ✅ redirect 301 for location missing final `/`
        ✅ redirect:
                - double quotes in string argument of config file, should they stay ?
                - we should allow empty argument : `return 404;`
                        Syntax:	return code [text];
                                return code URL;
                                return URL; <- pas webserv
        ✅ remove autoindex inheritence in location ?...
        ✅ POST

         GET with query -> to be tested !

        - Version: rfc (client ><= server) + status 505.
                - send 426 Upgrade Required if 1.0 ? https://httpwg.org/specs/rfc7231.html#status.426


Calixte :
        - Your program should call the cgi with the file requested as first argument = ask Prawda

        - cgi = add post body

        - autoindex = filter files

        - free all allocations


Network: close fd on send response when not successful ?

Delete :
		- main_brian.cpp ?
		- main.cpp => includes qui sert a rien
		- enleve les todos dans les fichiers

Check copy sockets

all :
        - You should never do a read operation or a write operation without going through
                poll (or equivalent).
        - Leaks
        - request timeout system (to do)
        - HARD TEST DELETE && GET && EVERYTHING
        - Python-CGI
        - test webserv with tester

why not ? :
        - implement non chunked cgi reponse for fast cgi/small responses (= content_length in the subject)
